# Copy to frontend/.env.local and restart `bun run desktop`.
# Forces the Hardware onboarding step even when this PC meets min specs
# (also re-opens onboarding so you can preview after setup is done).
NEXT_PUBLIC_FORCE_ONBOARDING_SPECS=0
